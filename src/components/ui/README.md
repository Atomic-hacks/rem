# UI Primitives

Atomic, reusable presentation primitives live here: buttons, inputs, badges, dialogs, tabs, menus, form controls, and icon buttons.

Keep these components domain-agnostic. They should not import listing, search, agent, or map feature logic.
