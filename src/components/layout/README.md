# Layout Shells

Shared page chrome and layout primitives live here: marketplace shell, header slots, footer slots, sidebar shells, and responsive page frames.

Keep route-specific orchestration in `app` or `src/features/*`.
