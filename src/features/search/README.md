# Search Feature

Search-specific UI components belong here.

Keep shared hooks in `src/hooks`, shared helpers in `src/lib`, shared constants in `src/constants`, mock data in `src/data`, and all TypeScript contracts in `src/types`.
