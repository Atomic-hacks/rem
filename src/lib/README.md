# Shared Lib

Shared frontend helpers live here: formatting, guards, URL query parsing, map adapters, analytics event helpers, and small typed utilities.

Keep constants in `src/constants`, mock data in `src/data`, hooks in `src/hooks`, and type contracts in `src/types`.
